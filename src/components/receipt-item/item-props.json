{
  "collectionName": "components_receipt_item_item_props",
  "info": {
    "displayName": "Свойства позиции",
    "description": "Properties of a receipt item"
  },
  "options": {},
  "attributes": {
    "unitPrice": {
      "type": "decimal",
      "required": true
    },
    "quantity": {
      "type": "decimal",
      "required": true
    },
    "measureUnit": {
      "type": "string",
      "required": true
    },
    "totalPrice": {
      "type": "decimal",
      "required": true
    },
    "department": {
      "type": "string",
      "required": true
    },
    "ntin": {
      "type": "string",
      "required": false,
      "description": "NTIN/GTIN товара из фискальных данных чека, если ОФД его передал"
    },
    "itemizedPosition": {
      "type": "integer",
      "required": false,
      "description": "Номер позиции в товарном чеке (для фото-флоу) — чтобы админ быстро находил нужную строку на фото, не искал по названию"
    }
  }
}
